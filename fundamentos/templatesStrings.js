const nome = 'Jailson'

// Concatenação convencional
const concatenacao = 'Olá, ' + nome + '!'

// Template string
const template = `Olá ${nome}!`

// Template string (Com quebra de linha)

const templateComQuebra = `Olá, 
${nome}!`
console.log(templateComQuebra)